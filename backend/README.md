# Backend

The shared FastAPI backend lives in `backend/api`. B Core, AMD and Tokyo
extensions import this package rather than copying backend or provider code.

Run all setup and quality commands from the repository root. See
[`backend/api/README.md`](api/README.md) for the development command and
configuration contract.
